interface Stats {

    /** To get consumption for each vehicle, obvious comment, but i wanted to see that it travels with the heritance */
    float getFuelConsumption();

    /** To get maxspeed of each vehicle.
     * I never needed to use this method, but i will leave it anyway.*/
    int getmaxspeed();
}
